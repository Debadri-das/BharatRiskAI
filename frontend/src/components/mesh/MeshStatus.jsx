export default function MeshStatus({ route = [] }) { return <section className="panel" style={{ padding: 16 }}><h3 style={{ marginTop: 0 }}>Mesh Route</h3><div style={{ display: 'grid', gap: 8 }}>{(route.length ? route : ['PHONE A', 'PHONE B', 'PHONE C', 'GATEWAY'].map((node) => ({ node, status: 'READY', packet: { ttl: 10, hop_count: 0 } }))).map((hop) => <div key={hop.node} style={{ display: 'flex', justifyContent: 'space-between' }}><b>{hop.node}</b><span>{hop.status} ttl {hop.packet.ttl} hop {hop.packet.hop_count}</span></div>)}</div></section>; }
