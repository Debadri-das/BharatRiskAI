import { Clock3, Droplets, FileText, MapPin } from 'lucide-react';

export default function ReportList({ reports = [] }) { return <section className="records-panel panel"><div className="records-heading"><div><span className="section-kicker">SUPABASE RECORDS</span><h2>Latest citizen reports</h2><p>Ground observations saved to the shared response database.</p></div><span className="record-count">{reports.length} reports</span></div>{reports.length === 0 ? <p className="records-empty">No citizen reports have been submitted yet.</p> : <div className="record-list">{reports.map((report) => <article className="record-item" key={report.id}><div className={`record-icon ${String(report.severity).toLowerCase()}`}><FileText size={16} /></div><div className="record-body"><div className="record-topline"><strong>{report.severity}</strong><span className="record-measure"><Droplets size={13} /> {report.water_level_cm} cm</span></div><p>{report.description}</p><small><MapPin size={12} /> Zone {report.zone_id || 'unassigned'} <Clock3 size={12} /> {report.created_at ? new Date(report.created_at).toLocaleString() : 'Recently received'}</small></div></article>)}</div>}</section>; }
